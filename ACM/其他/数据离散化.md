###数据离散化

```c++
   sort(s+1,s+n+1);//第一步排序
   int size=unique(s+1,s+n+1)-s-1;//unique显示去重后的个数
    for(int i=1;i<=n;i++) {
        lsh[i]=lower_bound(s+1,s+size+1,scopy[i])-s;//即lsh为lshcopy离散化后对应的值 
        printf("%d ",lsh[i]);
```

